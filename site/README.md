# Site source

This directory contains Farman’s current portfolio. See the [main README](../README.md) for local development, editing, appearance controls, and publishing instructions.

Edit `index.html` for page content, `styles.css` for presentation, and `app.js` for research briefs and seasonal behavior. Store images in `assets/`.
