# Farman's notebook

The redesigned site lives in `site/`. Original Gatsby source remains in `src/` for reference.

- `npm start` starts the local preview at http://localhost:3000 (Node 18+; no install needed).
- `npm run build` copies the static site into `public/` for hosting.
- `npm run serve` previews the built output.

Edit `index.html` for homepage content, `styles.css` for design, and `app.js` for research briefs. Briefs are explicitly proposed research directions, not published essays. Professional results come from the provided résumé. No private résumé file or contact phone number has been copied into the site.

Fonts load from Google Fonts with local system fallbacks. The portrait is a local copy of Farman’s LinkedIn profile photo. The illustrations are original inline SVGs. Original Gatsby commands are retained as `legacy:develop` and `legacy:build`; they require the old dependencies. No deployment was performed.
